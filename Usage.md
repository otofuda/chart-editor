# Otofuda Chart Editor V2 — 使い方ガイド

音札向けの譜面エディタです。このドキュメントでは各機能の使い方を説明します。

---

## 1. ファイルの読み込みと保存

### 譜面ファイルを開く
画面上部の「**ファイルを選択**」から譜面JSONファイルを選択します。
新規作成の場合はファイルを選択せずに譜面制作を開始できます。

### 譜面ファイルを保存する
右下の「**名前を付けて保存**」ボタンをクリックすると、現在の譜面データがJSONファイルとしてダウンロードされます。

### バックアップ
Ctrl+S（Cmd+S）で現在の譜面データをブラウザのローカルストレージにバックアップできます。

### バックアップから復元する
「**バックアップから復元**」ボタンをクリックすると、ブラウザのローカルストレージに保存されたバックアップデータから譜面を復元します。

---

## 2. 難易度の選択

画面上部の難易度ボタン（**RAKU / EASY / NORMAL / HARD / EXTRA**）または「難易度」ドロップダウンで編集対象の難易度を切り替えます。各難易度は独立したノーツデータを持ちます。

---

## 3. 譜面情報の設定

「**譜面情報**」セクションで以下の項目を設定します。

| 項目 | 説明 |
|------|------|
| **オフセット** | 楽曲開始タイミングのずれ（ミリ秒） |
| **BPM** | 楽曲のテンポ（拍/分） |
| **拍子** | 1小節あたりの拍数（例: 4/4拍子 → 4） |
| **譜面ver** | 譜面フォーマットのバージョン（自動設定・変更不可） |

---

## 4. ノート挿入モード

「**ノート挿入モード**」チェックボックスを有効にすると、ノート挿入パネルが表示されます。

### ノートの配置手順

1. **ノート種別**を選択します（例: 通常、ロング、フリック等）
2. **measure**（小節番号）、**position**（小節内の位置）、**split**（分割数）を設定します
3. **レーン**（1〜5）をラジオボタンで選択します
4. 「**ノートを仮配置**」ボタン（または `Enter` キー）を押して仮配置します
5. 仮配置したノートは橙色のバッジ数で確認できます
6. 「**挿入する**」ボタンを押すと譜面に確定挿入されます

### キーボードショートカット（position フィールド）

position フィールドにフォーカスしている状態で以下のキー操作が可能です。

| キー | 動作 |
|------|------|
| `Enter` | 仮配置（ノートを仮配置ボタンと同じ） |
| `←` `→` | レーンを左右に移動 |
| `↑` `↓` | 位置を上下（前後）に移動 |

### position と split について

- `split` は1小節をいくつに分割するかを表します（例: 4分音符なら split=4）
- `position` は0から split-1 の値で小節内の位置を指定します

### 全難易度への同時挿入

「**このノートのみを全難易度に同時挿入**」ボタンを押すと、現在の設定で全難易度（RAKU/EASY/NORMAL/HARD/EXTRA）に同じノートを一括挿入できます。

---

## 5. ノートの種類

主なノートタイプは以下の通りです。

| Type | 種別 | 説明 |
|------|------|------|
| 1 | 通常 | 通常のノート |
| 2 | ロング | 長押しノート（終点を設定する必要があります） |
| 5 | 音札 | 札を場に出す特殊ノート |

---

## 6. ロングノーツの編集

ノート種別に「**ロング（Type 2）**」を選択すると、「ロングノーツ編集」セクションが表示されます。

- 「**終点を追加**」ボタンで終点を追加します
- 各終点には独立して measure・position・split・lane を設定できます
- 終点が1つもない場合は警告が表示されます

---

## 7. テクスチャ（Type 94）

ノート種別で「テクスチャ（Type 94）」を選択すると、テクスチャ選択メニューが表示されます。

---

## 8. プレビュー表示設定

「**プレビュー領域の設定**」セクションで以下を調整できます。

### ハイスピード調整

スライダーで `beatHeight`（1ピクセルあたりの拍の高さ）を調整します。
現在のハイスピード速度値 = `BPM × beatHeight ÷ 100` として表示されます。

### 楽曲ファイルの読み込み

「**楽曲ファイル選択**」から音楽ファイル（mp3等）を読み込むと、プレビュー時に楽曲を再生できます。
隣のスライダーで音量を調整できます。

### 小節へのジャンプ

「**○小節へ**」入力欄に小節番号を入力して「遷移」ボタンを押すと、その小節へスクロールします。

### 表示モード

| モード | 説明 |
|--------|------|
| **ノーツ詳細を表示** | ノートのType・lane・measure等の詳細情報を表示します |
| **キャプチャ用モード** | スクリーンショット向けの表示に切り替えます |
| **譜面プレビュー用** | プレビュー再生モードに切り替えます |
| **譜面画像生成用** | 譜面の画像書き出し向けの表示に切り替えます |
| **譜面停止をシミュレート** | 譜面停止の効果をプレビューでシミュレートします |

---

## 9. 選択機能と一括操作

### ノーツの一括選択

「**ノーツの一括選択**」ボタンを押すと、以下の条件でノーツをまとめて選択できます。

- **対象Type**: チェックボックスで選択したいノートタイプを指定
- **対象小節**: 開始〜終了小節の範囲を指定（終了を0にすると最後まで対象）
- 「すべて選択する」ボタンで実行

### 選択解除

「**すべて選択解除**」ボタンで現在の選択状態をクリアします。

### 選択ノーツを削除

「**選択ノーツを削除**」ボタンを押すと確認ダイアログが表示され、選択中のノーツをすべて削除できます。

### ノーツの一括操作

「**ノーツの一括操作**」ボタンを押すと、選択ノーツに対して以下の操作が可能です。

| 操作 | 説明 |
|------|------|
| **種別の変更** | 選択ノーツのTypeを一括変更（ロング・音札を除く） |
| **レーンの変更** | 選択ノーツのレーンを指定レーンに一括変更（ロング・音札を除く） |
| **レーンの加算/減算** | 各ノーツのレーン番号を±4の範囲で加算/減算 |
| **小節の移動** | 選択ノーツの配置小節を一括でずらす |

---

## 10. 譜面分析

「**譜面分析**」ボタンを押すと、現在の難易度の譜面を分析した結果を表示します。

- **ノーツ数**: 判定ノーツ（通常、ロング、フリック等）の合計数
- **オブジェクト数**: 全オブジェクト（区切り線・コメントを含む）の合計数
- **平均密度**: 1秒あたりのノーツ数
- **密度グラフ**: 小節ごとのノーツ密度の推移（折れ線グラフ）
- **音札の位置**: 音札ノーツの配置を示すグラフ
- **種類別ノーツ数**: 各Typeのノーツ数と割合の一覧

---

## 11. メッセージログ

「**メッセージログ**」ボタンを押すとログパネルが開き、直近の操作履歴や通知メッセージを確認できます。

---

## 12. 譜面フォーマットについて

Otofudaの譜面フォーマット（NoteData型の仕様）については、以下を参照してください。

[chart-types — GitHub](https://github.com/otofuda/chart-types)

