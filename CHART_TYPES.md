## 音札譜面フォーマット

- 各難易度は「ノーツ(0個以上のノートの集合からなる)」の配列です
- 音符の「位置」とは、楽曲の中で音符が判定ラインに到達する時間のことです
- `info` キーに譜面情報が格納されています

## 音符について

### typeキー

|値|意味|備考|
|---|---|---|
|1|通常ノート||
|2|ロングノート|endキーに終点情報|
|3|左フリック|optionキー使用|
|4|右フリック|optionキー使用|
|5|音札ノート|全難易度で共通位置|
|6|上フリック|optionキー使用|
|7|下フリック|optionキー使用|
|89|中点 / 終端なし|ロングノーツの不可視中点・終端|
|90|ダミー|optionキー使用|
|91|特殊オブジェクト|optionキー使用|
|92|停止||
|93|瞬間移動|optionキーで拍数を指定|
|94|テクスチャ|optionキー使用|
|95|区切り線|optionキーで幅を指定|
|96|LED制御|optionキーでRGB指定|
|97|拍子変化|optionキーで拍数を指定|
|98|BPM変化|optionキーでBPM値を指定|
|99|EOF|終了位置 1難易度に1回のみ|
|100|コメント|optionキーでコメント指定|

- 今後、種類が増えるときは88, 87...と追加していきます

### laneキー

|1|2|3|4|5|
|---|---|---|---|---|
|壱|弐|参|肆|伍|

- type: 1 の時、音符のレーンを整数値(1〜5)で記述します
  - 左から右へ数字が大きくなります
- type: 2 およびロングノーツの終点・中継点（end内のノートやtype: 89）の時、レーンを数値（小数の指定が可能。0.0〜6.0）で記述します
  - 中間位置や緩やかな曲線の表現のため、小数値（例: `2.5`）を指定できます
- type: 3, 4, 6, 7, 94 の時、音符のレーンを整数値で記述します
  - `width`などのオプション(後述)を考慮した上で自由なレーンに音符を配置できます
- type: 95 の時、音符のレーンを整数値で記述します
  - `length`オプション(後述)を考慮した上で自由なレーンに音符を配置できます
- type: 5 の時、`3` を記述します
- 上記に該当しないtypeの場合はlaneキーは使用しません(`-1` とします)

### measureキー

- すべてのtypeの時、音符の位置の小節数を整数値で記述します

### position, splitキー

- すべてのtypeの時、小節内での位置を「position/split」となるよう記述します
  - 「position/split」の値は0以上1未満になるようにします(1.0は、次の小節の0.0です)
  - 小節の頭が0/1、ちょうど真ん中が1/2になります
  - つまり、4分音符のときはsplitが4に、8分音符のときは8になります
- `type:95` かつ `position: 0` の時は、当該小節の小節線を非表示にする効果を持ちます(小節線非表示制御)

### optionキー

optionキーはStringの配列です。

数値型のオプションでは、Floatにパースできる文字列を指定してください。

#### type: 1, 89（通常 / 終点・中点）

```
option: [speed: Float, orbit: Float, curve: String]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|speed|Float|No|スピード|
|1|orbit|Float|No|軌道(レーン幅/秒)|
|2|curve|String|No|親ノードからの曲線補間タイプ (`"linear"`, `"ease"`, `"easeIn"`, `"easeOut"`)。省略時は `"linear"`|

- 単体の通常ノート（`type: 1`）では `curve` は使用不可
- `speed`, `orbit`, `curve` のいずれも省略可能

#### type: 2（ロングノート始点）

```
option: [speed: Float, orbit: Float]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|speed|Float|No|スピード|
|1|orbit|Float|No|軌道(レーン幅/秒)|

- `speed`, `orbit` は省略可能

#### type: 3, 4, 6, 7（フリック）

```
option: [width: Float, offsetNumer: Integer, offsetDenom: Integer, speed: Float, orbit: Float]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|width|Float|Yes|中心を基点とした横幅(単位：1レーンの幅)|
|1|offsetNumer|Integer|No|右側へのオフセット分数の分子|
|2|offsetDenom|Integer|No|右側へのオフセット分数の分母|
|3|speed|Float|No|スピード|
|4|orbit|Float|No|軌道(レーン幅/秒)|

- 注：ノートは中心を基点として `width` レーン分の横幅を持ちます。`-1` の時は通常の幅(3レーン分)になります
- 注：ノートは `offsetNumer/offsetDenom` レーン分、オフセットとして右側に移動します

#### type: 5（音札）

```
option: [speed: Float]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|speed|Float|No|スピード|

#### type: 90（ダミー）

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|type|String|Yes|擬態する音符の種類|

- 添字 `0` に擬態対象の音符種類(typeキーに指定するもの)を指定します
- 添字 `1` 以降には、擬態対象のtypeで必要になる `option` の添字＋1の位置に指定します
- `type: 1, 2, 3, 4, 5` に対応しています

#### type: 91（特殊オブジェクト）

```
option: [objectType: String, objectName: String, option: String]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|objectType|String|Yes|表示するオブジェクトの種類|
|1|objectName|String|No|objectTypeのうち、表示するオブジェクトの名前(指定する必要がある場合のみ)|
|2|option|String|No|その他フラグ(指定する必要がある場合のみ)|

#### type: 93（瞬間移動）

```
option: [beat: Integer]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|beat|Integer|Yes|瞬間移動する拍数|

- 注：この小節が `beatChange/4` 拍ぶんの長さになり、先頭から末尾まで瞬間移動します

#### type: 94（テクスチャ）

```
option: [source: String, width: Float, height: Float, offsetNumer: Integer, offsetDenom: Integer, speed: Float, orbit: Float]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|source|String|Yes|画像のソース(httpまたはhttpsプロトコル)|
|1|width|Float|Yes|中心を基点とした横幅(単位：1レーンの幅)|
|2|height|Float|Yes|下面を基点とした高さ(単位：1小節の高さ)|
|3|offsetNumer|Integer|No|右側へのオフセット分数の分子|
|4|offsetDenom|Integer|No|右側へのオフセット分数の分母|
|5|speed|Float|No|スピード|
|6|orbit|Float|No|軌道(レーン幅/秒)|

- 注：ノートは中心を基点として `width` レーン分の横幅を持ちます(小数を使用可能)。 `-1` で通常の幅(3レーン分)になります
- 注：ノートは `offsetNumer/offsetDenom` レーン分オフセットとして右に移動します(両方の値は正の整数)

#### type: 95（区切り線）

```
option: [length: Integer, speed: Float, orbit: Float]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|length|Integer|Yes|左面を基点とした横幅(単位：1レーンの幅)|
|1|speed|Float|No|スピード|
|2|orbit|Float|No|軌道(レーン幅/秒)|

- 注：ノートは `lane` を起点として右側に `length` レーン分の長さを持ちます（値は-1または、1～5）
- 言い換えると、 `lane` 番レーンの左端から `lane + length - 1` 番レーンの右端まで区切り線が描画されます
- 小節線非表示制御のときは `-1` を指定します

#### type: 96（LED制御）

```
option: [red: Integer, green: Integer, blue: Integer]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|red|Integer|Yes|赤色輝度(0〜255の整数)|
|1|green|Integer|Yes|緑色輝度(0〜255の整数)|
|2|blue|Integer|Yes|青色輝度(0〜255の整数)|

#### type: 97（拍子変化）

```
option: [beat: Integer]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|beat|Integer|Yes|変化後の **n/4拍子** の分子の値|

- 注：このノートの後、 `beatChange/4` 拍子になります

#### type: 98（BPM変化）

```
option: [bpm: Float]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|bpm|Float|Yes|変化後のBPMの値|

- 注：このノートの後、`bpmChange` BPMになります

#### type: 100（コメント）

```
option: [comment: String]
```

|添字|オプション名|型|必須|説明|
|---|---|---|---|---|
|0|comment|String|Yes|コメント文字列|

上記以外の場合は空の配列 `[]` とします

### endキー

`type: 2`（ロングノート始点）または `end` 内のノードにおいて、その音符から接続する終点・中点を表す「ノートオブジェクトの配列」を記述します

#### 再帰構造

`end` は再帰的なツリー構造を持ちます。各終点ノード自身がさらに `end` 配列を持つことができます

- **多段ロングノーツ（中点）**:
  始点ノードの `end` に中点ノードを含め、その中点ノードの `end` に次の終点ノードを指定することで、折れ曲がる連続したロングノーツを表現します
- **分岐ロングノーツ**:
  `end` 配列内に複数のノードを記述することで、1つの始点から複数本の帯へと分岐するロングノーツを表現できます

##### type の挙動

- **中点（子要素 `end` を持つノード）の場合**:
  - `type: 1`: **コンボ加算中点**（ロングノーツの帯上に小さな〇が表示され、通過時にコンボ加算あり）
  - `type: 89`: **不可視中点**（〇は描画されず、帯の折れ曲がり・カーブ制御のみを実行。コンボ加算なし）
- **終点（子要素 `end` を持たないノード）の場合**:
  - `type: 1`: **通常終点**（通常の終点。コンボ加算あり）
  - `type: 89`: **終端なし**（離す判定がない終点。コンボ加算あり）

#### 曲線補間（curve）

`option[2]` に指定された文字列に応じて、親ノードからそのノードまでの帯の形状が補間されます。

|値|名称|説明|
|---|---|---|
|`linear`|直線 (Linear)|親ノードからこのノードまでを直線（台形ポリゴン）で結びます（デフォルト）|
|`ease`|S字カーブ (Ease In-Out)|始点・終点の傾きを垂直に保ち、中間で滑らかにレーンを移動するS字ベジェ曲線を描きます|
|`easeIn`|Ease In|始点は垂直に出発し、終点に向かって徐々に曲がるベジェ曲線を描きます|
|`easeOut`|Ease Out|始点から曲がり始め、終点手前で垂直に着地するベジェ曲線を描きます|

#### 注意事項

- **時間順序**: 各終点・中継点の位置（`measure + position / split`）は、必ず接続元となる親ノードの位置よりも未来（大きい値）である必要があります
- ロングノーツ以外のノートでは、`end` キーは使用しません（空の配列 `[]` とします）
